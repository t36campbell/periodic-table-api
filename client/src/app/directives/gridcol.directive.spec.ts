import { GridcolDirective } from './gridcol.directive';

describe('GridcolDirective', () => {
  it('should create an instance', () => {
    const directive = new GridcolDirective();
    expect(directive).toBeTruthy();
  });
});
